<div id="agents_register_new" class="container">
  <div class='row'>
  <div class='col-sm-12'>
    <h2 >Sign up</h2>
    In order to contribute literature information to ClinGen curation activities or request a curation, you must create an account.  This allows us to contact you if we have questions about the information you provide, let you know the outcome of your request, etc. 
    <hr />
    </div>
    <br clear="both" />
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <div class="col-sm-6">
        <div class="form-group">
          <%= f.label :first_name, :class => "control-label"  %>
          <%= f.text_field :first_name, autofocus: true, class: "form-control", placeholder: "First Name", required: "required" %>
        </div>
      </div>
      <div class="col-sm-6">
        <div class="form-group">
          <%= f.label :last_name, :class => "control-label"  %>
          <%= f.text_field :last_name, autofocus: true, class: "form-control", placeholder: "Last Name", required: "required" %>
        </div>
      </div>
      <div class="col-sm-6">
      <div class="form-group">
        <%= f.label :institution, :class => "control-label"  %>
        <%= f.text_field :institution, autofocus: true, class: "form-control", placeholder: "Institution", required: "required" %>
      </div>
      </div>
      <div class="col-sm-6">

      <div class="form-group">
        <%= f.label :title, :class => "control-label"  %>
        <%= f.text_field :title, autofocus: true, class: "form-control", placeholder: "Professional Title", required: "required" %>
      </div>
      </div>
      <div class="col-sm-6">

      <div class="form-group">
        <%= f.label :degrees, :class => "control-label"  %>
        <%= f.text_field :degrees, autofocus: true, class: "form-control", placeholder: "Professional Degrees", required: "required" %>
      </div>
      </div>
      <div class="col-sm-6">


      <div class="form-group">
        <%= f.label :email, :class => "control-label"  %>
        <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "Email", required: "required" %>
      </div>
      </div>
      <div class="col-sm-6">

      <div class="form-group">
        <%= f.label :password, :class => "control-label"  %>
        <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Password", required: "required" %>
        <% if @minimum_password_length %>
          <p class="help-block">(<%= @minimum_password_length %> characters minimum)</p>
        <% end %>
      </div>
      </div>
      <div class="col-sm-6">

      <div class="form-group">
        <%= f.label :password_confirmation, :class => "control-label"  %>
        <%= f.password_field :password_confirmation, autocomplete: "off",  class: "form-control", placeholder: "Confirm", required: "required" %>
      </div>
      </div>

      <div class="col-sm-6 clear-left">
      <div class="form-group">
        <div class="checkbox">
          <label>
            <%= f.check_box :clingen_newsletter, autocomplete: "off", checked: "checked"  %>
            YES, sign me up to receive periodic email updates from ClinGen 
            </label>
          </div>
      </div>
      </div>

      <div class="col-sm-12">
        <%= render "devise/shared/links" %>
        <%= f.submit "Sign up", :class => "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
