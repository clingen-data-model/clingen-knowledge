<div id="conditions_index" class="container">

  <h2>Conditions</h2>
  <% @conditions.each do |n|  %>
    <%= render "shared/result_list_condition", condition: n %>
  <% end %>
  <%= paginate @conditions %>
</div>
