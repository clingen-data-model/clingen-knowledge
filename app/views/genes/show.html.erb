<div id="genes_show" class="container">
</div>
<div id="genes_show_summary" role="tabpanel" class="tab-pane active  container results_curation_summary">
  <div class="row">
    <div class="col-sm-12">
      <h2 class="padding-top-xs">
        <%= render 'shared/subscribable_entity_header', entity: @gene %>

        <strong><%= @gene.symbol %></strong>
      </h2>
      <%= render 'gene_facts' %>

    </div>
  </div>
  <div class="col-sm-12">
    <div class="">
      <ul class="nav nav-tabs">        
        <li class="active">
          <a href="#summary">
            ClinGen's Curation Summaries
          </a>
        </li> 
        <!-- <li class="">
             <a href="#variants">
             ClinVar Variants <span class="badge">#####</span>
             </a>
             </li> -->
        <li class="">
          <%= link_to "External Genomic Resources", gene_external_resources_genes_path(@gene) %>
        </li>
        <li class="">
          <a href="https://www.ncbi.nlm.nih.gov/clinvar/?term=<%= @gene.symbol %>%5Bgene%5D" class="" target="clinvar">ClinVar Variants  <i class="glyphicon glyphicon-new-window text-xs" id="external_clinvar_gene_variants"></i></a>
        </li>

      </ul>
      <div class="panel panel-default">
        <div class="panel-heading" id='results_curation_summary_heading'>
          <div class="row"><div class="col-sm-2">
            Condition
          </div>
          <div class="col-sm-10 ">
            <span class="border-left">ClinGen Curation Results</span>
          </div>
          </div>
        </div>
        <div id='results_curation_summary_details' class="panel-body results_curation_summary_details">
          <% if @diseases.present? || @gene.dosage_assertions.present? %>
            <% if @diseases.present? %>
              <% @diseases.each do |d| %>
                <%= render 'disease_detail', disease: d %>
              <% end %>
            <% end %>
            <hr class="results_curation_summary_details_spacer" />
            <% if @gene.dosage_assertions.present? %>
                <%= render 'gene_dosage_no_condition'%>
            <% end %>
            <hr class="results_curation_summary_details_spacer" />
          <% else %>
            <div class="alert alert-info text-center" role="alert">
              <strong><%= @gene.symbol %></strong> has not been curated with a condition.  <%= link_to "ClinGen compiled a list of external genomic resources", gene_external_resources_genes_path(@gene) %> with direct links to information about <%= @gene.symbol %>.
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
